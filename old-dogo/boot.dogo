:for [a b]
  # a: src
  # b: *(name src)
  #
  :in 
   {
    +c (do .dogo .a)
    +d
      (do .lap
        .b
        :for [e f]
          (.e (do .dogo .f))
      )
   }
    :by
      :to [e.[s f]]
        (do .sibl
          (%skin %seed %0)
          (%fire .c (%lib .d))
        )
      .e.f
