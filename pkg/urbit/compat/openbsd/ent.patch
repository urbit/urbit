diff --git a/configure b/configure
--- a/configure
+++ b/configure
@@ -14,7 +14,7 @@
 
     log "Trying IMPL=$IMPL"
 
-    if IMPL=$impl make >/dev/null 2>/dev/null
+    if IMPL=$impl gmake >/dev/null 2>/dev/null
     then sed -i 's|$(error IMPL must be set)|IMPL='"$impl"'|' Makefile
          log "IMPL=$IMPL works"
          exit 0
