{$ $ $ $ $}

Base
  { loft =
      Cell
        Type
        (Scope
           (Look
              (F (Cell
                    Type
                    (Scope
                       (Look
                          (F (Cell
                                Type (Scope (Look (F (Cell Type (Scope (Look (F Type)))))))))))))))
  }