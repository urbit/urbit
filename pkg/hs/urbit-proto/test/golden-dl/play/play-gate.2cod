:*  $-(@ @)
    $-(a|@ @)
    $-(a|$ a)
    $-(a|{@ $} +3.a)
    $-(a|$-(@ $) $-(b|@ (a b)))
==

Cons
  (Gate (Aura "") (Scope (Look (F (Aura "")))))
  (Cons
     (Gate (Mask "a" (Aura "")) (Scope (Aura "")))
     (Cons
        (Gate (Mask "a" Type) (Scope (Wing [] (Look (B ())))))
        (Cons
           (Gate
              (Mask "a" (Cell (Aura "") (Scope (Look (F Type)))))
              (Scope (Wing [ Axis 3 ] (Look (B ())))))
           (Gate
              (Mask "a" (Gate (Aura "") (Scope (Look (F Type)))))
              (Scope
                 (Gate
                    (Mask "b" (Aura ""))
                    (Scope
                       (Slam
                          (Wing [] (Look (F (Look (B ()))))) (Wing [] (Look (B ())))))))))))