/@  chat
:-  [%chat %node]
|=  cat=chat
^-  manx
;dl
  ;dt: Title
  ;dd: {(trip title.cat)}
  ;dt: Allowed
  ;dd 
    ;ul
      ;*  
      %+  turn  ~(tap in who.cat)
      |=  s=ship
      ;code.ship: {(scow %p s)}
    ==
  ==
==
