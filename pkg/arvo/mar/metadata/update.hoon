/-  store=metadata-store
/+  *metadata-json
|_  =update:store
++  grad  %noun
++  grow
  |%
  ++  noun  update
  ++  resource
    ?>  ?=(?(%add %remove %initial-group) -.update)
    group.update
  ++  json  (update-to-json update)
  --
::
++  grab
  |%
  ++  noun  update:store
  ++  json  json-to-action
  --
::
--
