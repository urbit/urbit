/-  spider
:-  %say
|=  [[now=@da *] ~ *]
:-  %tang
=/  tree
  .^((list (list @ud)) %mx /=spider/(scot %da now)/tree/noun)
%+  turn  tree
|=  imp=(list @ud)
=/  =path
  (turn imp |=(=@ud (scot %ud ud)))
>path<
