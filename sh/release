#!/usr/bin/env bash

set -e

ver="${TRAVIS_TAG-${TRAVIS_COMMIT-$(git rev-parse HEAD)}}"

mkdir -p release

for plat in linux64 darwin
do
  sh/cross urbit "$plat"

  echo "packaging release/urbit-$plat-$ver.tar.xz"
  (cd cross/$plat; tar c .) |
    xz > release/urbit-$plat-$ver.tar.xz
done
