%dtps  .+  "dotpes"

define:
    [%dtps p=(gene)]

direct:
    [%dtps *]
  :-  (nice [%atom %%])
  (boil |.([4 q:$$(gen p.gen, gol [%atom %%])]))

custom:
    (stag %dtps ;~(pfix pes (ifix [lep pel] wide)))
