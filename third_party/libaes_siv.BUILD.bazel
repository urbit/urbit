load("@rules_cc//cc:defs.bzl", "cc_library")

genrule(
    name = "config",
    outs = ["config.h"],
    cmd = "touch $@",
)

cc_library(
    visibility = ["//visibility:public"],
    name = "libaes_siv",
    srcs = [
        "aes_siv.c",
        ":config.h",
    ],
    hdrs = ["aes_siv.h"],
    deps = ["@openssl"],
    strip_include_prefix = "",
)
