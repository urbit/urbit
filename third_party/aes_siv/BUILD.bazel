load("@rules_cc//cc:defs.bzl", "cc_library")

genrule(
    name = "config",
    outs = ["config.h"],
    cmd = "touch $@",
)

cc_library(
    name = "aes_siv",
    srcs = [
        "aes_siv.c",
        ":config.h",
    ],
    hdrs = ["aes_siv.h"],
    includes = ["."],
    visibility = ["//visibility:public"],
    deps = ["@openssl"],
)
