load("@rules_foreign_cc//foreign_cc:defs.bzl", "make")

filegroup(
    name = "srcs",
    srcs = glob(["**"]),
)

make(
    name = "softfloat",
    args = ["-C"] + select({
        "//conditions:default": ["build/Linux-x86_64-GCC"],
    }),
    includes = ["source/include"],
    lib_source = ":srcs",
    out_static_libs = select({
        "//conditions:default": ["softfloat.a"],
    }),
    visibility = ["//visibility:public"],
)
